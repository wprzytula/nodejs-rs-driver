[package]
edition = "2024"
name = "scylladb-javascript-driver"
version = "0.6.1"

[lib]
crate-type = ["cdylib"]

[features]
# Swaps in a counting global allocator plus its readout, used by `benchmark/napi-call/run.js` to
# assert how many Rust allocations a boundary crossing performs. Off by default so that release
# builds pay no atomics per allocation.
bench-alloc = []

[dependencies]

napi = { version = "3.6.0", default-features = false, features = ["napi4", "napi6", "async"] }
napi-derive = "3.4.0"
scylla = { version = "1.7.0", features = ["num-bigint-03", "openssl-010", "unstable-nodejs-rs"] }
tokio = { version = "1.34", features = ["full"] }
futures = "0.3"
uuid = "1"
regex = "1.11.1"
thiserror = "2.0.12"
num-bigint = "0.4.6"
openssl = "0.10.70"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["registry"] }

[lints.rust]
unsafe-op-in-unsafe-fn = "warn"

[build-dependencies]
napi-build = "2.1.5"

[profile.release]
lto = true
panic = "abort"

[profile.dev]
panic = "abort"

